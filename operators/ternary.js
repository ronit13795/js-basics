
// if customer has more than 100 pointts,
// they are 'gold' customers, otherwise,
// they are 'silver' customers

let points = 100;
let type = points > 100 ? 'gold' : 'silver'
console.log(type);

// if person is older than 18 his access granted
// otherwise its not granted
let age = 40
let name = 'daniel'
let access = age >= 18 ? "granted" : "not granted"

console.log(access);

