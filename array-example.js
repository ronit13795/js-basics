let arr = [] //initialize(לאתחל) empty array(מערך)
let arr1 = ['red', 'blue'] //initialize array with 2 strings inside

// you can add diffrent types in same array - initialize array with 3 items 2 numbers and 1 string
let arr2 = [1, 2, 'green', false]

//print the whole array:
console.log(arr1);

// to get one element we need to specify the index number e.g. arr1[0] will give us 'red'
console.log(arr[1]); // 'blue'


// add new item to the array 
arr1[2] = "black" // ['red','blue','black']

// to get the length of array
console.log(arr1.length);  // will print 3