const multiple = (x, y) => {
    return x * y
}

const subtract = (x, y) => {
    return x - y
}

const plus = (x, y) => {
    return x + y
}

const divide = (x, y) => {
    return x / y
}

const math = 'ex'

// console.log(module);

module.exports = {
    multiple,
    subtract,
    plus,
    divide,
    math
}